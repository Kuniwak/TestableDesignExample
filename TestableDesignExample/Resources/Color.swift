import UIKit



enum ColorPalette {
    enum Form {
        enum Background {
            static let normal = UIColor(
                hue: 0.666,
                saturation: 0.020,
                brightness: 0.960,
                alpha: 0
            )


            static let ng = UIColor(
                hue: 0.005,
                saturation: 0.280,
                brightness: 1,
                alpha: 1
            )


            static let ok = UIColor(
                hue: 0.311,
                saturation: 0.280,
                brightness: 1,
                alpha: 1
            )
        }
    }
}